syntax = "proto3";

message ResponseStatus {
    int32 statusCode = 1;
    oneof message {
        string responseMessage = 2;
        string errorMessage = 3;
    }
}

message id {
    string id = 1;
}

message name {
    string name = 1;
}
